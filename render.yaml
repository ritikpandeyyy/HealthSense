services:
  - type: web
    name: healthsense-ai
    runtime: docker
    plan: starter
    dockerFilePath: Dockerfile
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATA_DIR
        value: /tmp/data-store
      - key: ADMIN_EMAIL
        value: ritikpandey9536@gmail.com
      - key: SENDGRID_API_KEY
        sync: false
      - key: MAIL_FROM
        sync: false
